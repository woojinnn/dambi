{
  "name": "policy-builder-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:wasm": "bash ../../scripts/wasm-build-policy-builder.sh",
    "dev": "yarn prepare:wasm && vite",
    "build": "yarn prepare:wasm && tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
