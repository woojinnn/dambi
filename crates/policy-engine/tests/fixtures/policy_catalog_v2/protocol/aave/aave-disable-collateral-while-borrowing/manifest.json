{
  "id": "aave-disable-collateral-while-borrowing",
  "schema_version": 2,
  "trigger": {
    "where": {
      "action.tag": {
        "eq": "disable_collateral"
      }
    }
  },
  "policy_rpc": [
    {
      "id": "post-disable-hf",
      "method": "lending.health_factor",
      "params": {
        "action": "$.action",
        "chain_id": "$.root.chain_id",
        "owner": "$.root.from",
        "venue": "$.action.venue"
      },
      "outputs": [
        {
          "kind": "context",
          "field": "postActionHf",
          "type": "Decimal",
          "from": "$.result.postActionHf",
          "required": false
        }
      ],
      "optional": true
    }
  ],
  "custom_context": {
    "fields": {
      "postActionHf": "decimal"
    }
  }
}
